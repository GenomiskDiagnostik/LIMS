# GlimR Test Matrix

## Status
- Matrix status: active
- Purpose: define the minimum test expectations that must be referenced before changes are implemented or released

## How to use this matrix
- Identify the touched rows before implementation.
- Treat rows marked "Yes" under release blocker as mandatory for release when the area is changed.
- If current coverage is partial, document the exact gap before changing behavior.

| Area | Code evidence | Current coverage | Required tests before change | Release blocker | Known gaps |
| --- | --- | --- | --- | --- | --- |
| PMB validation | `dataValidation.pmb`, `digits12` preset, `samples.pmb_number` unique index | Partial. Self-test now covers valid/invalid 12-digit PMB values, decimal-like rejection, default display label, custom label override, and duplicate create/edit behavior in sample flows. | Accept valid 12 digits, reject decimal-like and other invalid shapes when enabled, verify duplicate handling, verify create and edit flows, and verify display-label overrides in relevant forms. | Yes | No baseline suite yet for every PMB entry path, import path, or report/export surface. |
| CPR validation | `dataValidation.mrn`, `parseCprNumber()`, `applyCprAutofill()` | Partial. Self-test now covers valid/invalid CPR-like input, parser output, and autofill behavior. | Accept valid CPR-like input, reject invalid dates, verify century behavior, verify autofill and no unwanted overwrite of manual edits. | Yes | Current parser is heuristic, not a full CPR rules engine, and manual-override coverage is still missing. |
| CRUD smoke | Built-in self-test plus module CRUD flows | Present. Self-test exercises core create/update/delete paths, including the orders form with non-control sample selection and a legacy control-linked edit case. | Re-run self-test and targeted manual smoke for touched entities. | Yes | No formal external golden dataset harness yet. |
| Backup and restore roundtrip | `downloadBackup()`, `importData()`, `replaceAllData()`, `restoreFromFile()` | Partial. Self-test now covers `exportData()` plus `replaceAllData()` roundtrip with representative records, `validation_settings`, `presentation_branding_settings`, and `users.organization_logo_data_url`. | Export representative data, restore it, verify counts, key settings, presentation-only settings payload, presentation-only user payload, and verify no silent loss. | Yes | Browser download, file-picker restore, and store-by-store roundtrip coverage remain incomplete. |
| JSON export and import | JSON backup and bound-file paths | Partial. Core JSON snapshot/restore contract is now covered through self-test, including preservation of `presentation_branding_settings` and `users.organization_logo_data_url` in the JSON data path. | Verify backup file creation, re-import, compatibility, and data integrity. | Yes | Bound-file and manual restore still need explicit regression coverage. |
| CSV and ZIP export and import | CSV backup, per-entity CSV export, ZIP export, CSV restore | Partial. Runtime paths exist, and patient plus order filtered-export row counts and count-labels are now covered through self-test. | Verify filtered export counts, CSV restore behavior, ZIP contents, and no cross-store corruption. | Yes | CSV restore, ZIP contents, and filtered-export assertions outside patients and orders remain uncovered. |
| SQL export | `generateSqlDump()` and admin export UI | Present. Self-test exercises SQL export generation. | Verify export completes and output remains syntactically valid after touched changes. | No | Not an import path, but still sensitive to schema drift. |
| PDF generation | `createReportPdfBlob()`, custom PDF builder, report preview flow | Partial. Self-test now verifies PDF blob creation, MIME/signature, deterministic filename, per-user logo override, global logo fallback, local `logo.png` fallback, and embedded-SVG fallback behavior. | Generate PDF with representative report data, verify blob creation, filename, branding precedence, fallback order, and non-crash behavior. | Yes | No visual regression lock or snapshot baseline yet. |
| Presentation branding | `getPresentationSystemName()`, `applyPresentationBranding()`, dashboard print, planner mail, pedigree print, report PDF header | Partial. Self-test now covers default title fallback, global presentation label, custom per-user presentation label, and user-overrides-global behavior in the header and browser title. | Verify default, global, and per-user labels in header, browser title, KPI print, planner mail, pedigree print, and PDF subtitle. | Yes when touched | KPI print, planner mail, pedigree print, and PDF subtitle still rely on manual smoke. |
| Filtered export | `exportFilteredCsv()` and module-specific filtered arrays | Partial. Self-test now verifies row-count correctness for patient and order filtered export plus their matching export-button count labels. | Apply filters, export, and verify row count matches visible filtered rows and the UI count label where touched. | Yes | Samples, variants, reports, panels, audit, and variant-library exports still lack formal automated count assertions even though the labels are now present in UI. |
| AI-provider configuration | `ai_providers` store, admin provider UI, variant AI request flow | Partial. Configuration and request path exist, and self-test now verifies that the variant AI action renders as an icon button with a non-empty accessibility label while the provider select remains visible. | Verify provider CRUD, secret persistence behavior, icon-action accessibility, and no accidental hard dependency for core workflows. | Yes when touched | Live provider interoperability remains runtime-unverified. |
| Bound file and autosave | `bindDbFile()`, `saveBoundFile()`, autosave flags and settings | Partial. Full path exists. | Verify bind, save, reload, autosave on/off behavior, and no merge regression. | Yes | Long-running permission and conflict behavior is not yet locked. |
| Reset and restore | Reset, backup-first, import, replace-all flows | Partial. Paths exist. | Verify reset protections, backup creation, and successful restore to usable state. | Yes | Exact preservation semantics need formal assertions. |
| Audit logging | `audit` store and entity mutation flows | Partial. Runtime audit path exists, and self-test now verifies that `organization_logo_data_url` is omitted from `users` audit payloads and that logo-only updates do not create empty audit entries. | Verify create, update, delete, export, and restore-sensitive actions still log as expected when touched, and verify sensitive presentation-only payloads remain sanitized. | Yes when touched | No dedicated audit regression suite beyond runtime use. |
| Logo fallback | Header branding path, `loadReportLogoImage()`, and PDF generation path | Partial. Header UI and PDF now resolve branding in this order: per-user PNG logo, global PNG logo, local `logo.png`, embedded SVG fallback. Self-test verifies global and per-user override, local PNG fallback, and rasterized SVG fallback. | Verify user-overrides-global precedence, missing `logo.png` handling, and non-crash fallback in both UI and PDF. | Yes when logo or PDF behavior is touched | No visual snapshot baseline yet; the crop fix for the embedded SVG still requires manual smoke in browser and PDF viewers. |
| Family, patient, and order navigation | Existing cross-links in dashboard, families, samples, orders, reports | Partial. Cross-links exist, orders now include additive patient/family shortcuts, and self-test verifies that the new shortcut buttons render. | Verify touched navigation paths from orders to related entities and confirm no existing links break. | Yes when touched | Click-through behavior still relies on manual smoke; KPI drilldown remains intentionally limited to existing low-risk tab navigation. |
| Self-test coverage and gaps | Admin self-test flow | Present and expanded. Self-test now covers PMB, CPR, JSON roundtrip, PDF blob generation, per-user and global PDF/header logo precedence, local PNG and embedded SVG fallback, audit sanitization for user logos, patient and order filtered-export counts, matching export-button count text, order shortcut rendering, UI logo-fallback state, default/global/custom presentation labels, configurable PMB/MRN/Ekstra ID labels, orders sample-selector restrictions, legacy control-order edit safety, global branding form presence, and variant AI icon-button accessibility in addition to the earlier CRUD/FHIR/ZIP/SQL checks. | If touching covered areas, run self-test. If touching uncovered areas, add targeted tests or document why coverage remains manual. | Yes | CSV/ZIP restore, bound-file/autosave, visual PDF fidelity, full navigation click-through, presentation-label smoke outside header/title, and broader filtered-export coverage remain outside the current harness. |

## Matrix interpretation rule
If a change touches more than one row, the union of the required tests applies.
